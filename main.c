int main(int argc, char* argv[]) {
	for (/*every possible letter input*/) {
		//repeat this for loop for every letter since we don't know for sure
		//after choosing the letter (inside smallest for loop), we call md5 hash from a library

		//NOTE: Check the slide functions to see how the hashed passwords are stored
		//Potential: breaking the password up into parts ($id$salt$password) for less loops
	}
}